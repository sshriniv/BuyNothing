<%=image_tag @user.avatar.url(:original) %>
<h2> <%= @user.fname %> <%= @user.lname %></br></h2>
<h2><small>Email: </small><%= @user.email %></h2>

<hr size=2>
<h2><small>Groups</small></h2>

<% @groups.each do |group| %>
 <h3><%= link_to group.name, group %></h3>
<% end %>
</br></br>
<%= link_to "Edit Profile", edit_user_registration_path(current_user), class: "btn btn-primary"%>